*.iml
.gradle
.kotlin
/local.properties
/.idea/
.reasonix
.DS_Store
/build
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Signing — never commit keystore or its passwords
app/keystore.properties
app/keystore/

# Release build artifacts (only APK + changelog belong in git)
app/release/baselineProfiles/
app/release/output-metadata.json
